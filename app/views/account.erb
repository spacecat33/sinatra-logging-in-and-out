<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>title</title>
    <link rel="stylesheet" href="stylesheets/bootstrap.css" type="text/css">  
  </head>
  <body id="page-top">
    <% if Helpers.is_logged_in?(session) %>
      <h1>Welcome <%= @user.username %></h1> #i.e. Helpers.current_user(session).username
      <h3>Your Balance: <%= @user.balance.to_f %></h3> #i.e. Helpers.current_user(session).balance.to_f
    <% end %>

  <a href="/logout">Log Out</a>  

  </body>
</html>